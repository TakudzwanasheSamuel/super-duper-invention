export const PIN_KEY = 'user_pin';
export const LAST_ACTIVE_TIMESTAMP_KEY = 'last_active_timestamp';

/** How long the app can be backgrounded before it re-locks. */
export const PIN_LOCK_TIMEOUT_MS = 2 * 60 * 1000;
